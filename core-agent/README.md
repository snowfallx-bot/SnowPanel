# Core Agent

Minimal Rust bootstrap for SnowPanel core-agent.

## Run

`cargo run`

## Behavior

- Reads `CORE_AGENT_HOST` / `CORE_AGENT_PORT`
- Starts a basic TCP listener placeholder for future gRPC server wiring
